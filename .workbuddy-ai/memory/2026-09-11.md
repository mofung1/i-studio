# 2026-09-11

## 产品体验评审（PM 视角）

通读 PRD、apps/web、apps/server、packages/contracts、UI 原型与 20 份开发进度记录，产出 `docs/产品体验评审-2026-09-11.md`。

核心结论：主链路「上传 → 生成 → 下载」已通，但仍是"能演示、不能交付"状态。

主要发现：
- **P0 断点**：游客登录上下文全丢（workbench.tsx:157 直接跳登录，login 后写死跳 mode=general）；结果页只有下载，无变体/重跑/批量/加入项目；生成中用 Unsplash 假图 + 静态进度条伪造进度；AI 优化/商品分析/视觉拆解全是硬编码假功能；完全没有登出入口；点数计费体系零实现；注册无协议勾选。
- **P1**：首页"最近项目/发现灵感/1,280 点数"均为假数据；生成结果不落 asset 记录、资产无法复用到新任务；上传张数文案与实现不一致（说 4 张实为 1 张）；商品名称取文件名兜底 'Aero H1'、类目写死"其他"且不可填；刷新页面丢失任务状态；后端单 worker 串行队列无重试无部分成功；任务详情直接展示原始 input JSON；图片加载失败静默。
- **工程协作**：UI/ 原型（npm/vite 独立项目）与正式实现构成"双重真相"；PRD 100+ 条编号需求与 development-progress 之间无覆盖追踪；缺端到端冒烟；main.go 729 行且 db==nil 内存降级会静默丢任务。

建议顺序：先拆假数据止血（1 周）→ 补结果复用闭环与队列并发（2-3 周）→ 补点数计费与合规（2 周）。

## 交付给 Codex 的修复 prompt

产出 `docs/codex-修复prompt-2026-09-11.md`，含三段自包含 prompt：
- Prompt A（止血）：拆假数据 + 登录回跳 + 登出入口 + 生成中真实状态 + 文案对齐。仅前端改动，7 个任务。
- Prompt B（闭环）：结果区变体/重跑/加入项目、结果落 asset、工作台状态恢复与草稿、队列 worker 池 + 重试 + 部分成功、任务详情参数卡片化、图片加载失败可重试。
- Prompt C（计费合规）：wallets/credit_ledger/quotes 三表、quote 与幂等、冻结结算、协议勾选与 terms/privacy、风险词预检。

约定：不新增第三方依赖；不改 docs/PRD-* 与 UI/；点数值不硬编码（PRD 18.2 待定）；每批结束跑 pnpm typecheck/build + go test -race，并在 development-progress/ 追加记录。
