# 生图功能待办

盘点日期：2026-09-16。基线：拍平 monorepo 后的 `apps/web` + `apps/server`。

## 已完成、链路通的功能

- 通用生图：prompt、参考图（≤6 张）、风格、参考强度、4 种模型、5 种比例、1-16 张
- 商品主图 / 详情页：商品图上传、要求、10 种平台、9 种输出语言、模块选择
- 爆款复刻：商品图 + 1 张参考爆款、复刻程度（风格 / 高度）
- 产品精修：6 个优化项
- 结果区：下载、"用结果作参考图"再生成、重新生成
- 基础设施：任务队列、轮询、postgres 持久化、资产库、认证

---

## 待办 1：模块化差异化生成（第一期已完成）

**已完成（2026-09-16）**：后端按模块拆分生成批次，每个批次注入专属 prompt 指导。

- `apps/server/provider.go`：新增 `moduleHints`（15 个模块 → 自然语言生成指导）与 `allowedModules`；`promptForInput` 优先消费 `input["moduleHint"]`，替代原来的 `fmt.Sprint(counts)` 原始拼接
- `apps/server/queue.go`：`generateBatches` 拆为线性 / 模块化两条分支；custom 模式下 `generateModuleBatches` 按模块遍历分批（gpt batchSize=4，gemini=1），逐批注入 moduleHint
- `apps/server/validation.go`：补 moduleCounts key 合法性校验（`product-main` 5 个 / `detail-page` 14 个模块），堵住"任意 key 污染 prompt"
- 测试：`TestGenerateModuleBatches`（gpt 按模块分批、gemini 每批 1 张、smart 模式回归）+ 5 个校验用例，全量通过；Docker 重建后 api healthy；端到端 validate 接口验证非法 key 被拒、合法配置通过

**第二期（未做）**：
1. 结果区显示模块标签（目前结果只是图片列表，不知道哪张属于哪个模块）
2. UI 耗时提示（模块越多生成越慢，gemini 尤甚：5 模块×2 张 = 10 次供应商调用）

## 待办 2：发现灵感板块（纯占位）

**现状**：`apps/web/components/home-page.tsx` 的 inspiration 网格全部是 `inspiration-coming-soon`（"即将上线"）占位，数据是写死的前端数组，无后端接口、无真实内容。

**影响**：首页导航"发现灵感"指向的板块无实质内容。

**建议方向**：先定内容来源（运营录入 or 精选用户作品），再定接口。涉及新建后端接口 + 前端数据获取。

## 待办 3：prompt 拼装自然语言化（已完成）

**已完成（2026-09-16）**：`provider.go` 的 `promptForInput` 中 `enhancements` 从 `fmt.Sprint` 原始数组改为自然语言映射：

- 新增 `enhancementHints`（6 个选项 → 中文指导，与前端 `retouchOptions` 标签对齐）
- 拼装示例：`"产品精修要求：增强产品光泽与质感、修复划痕与瑕疵、净化背景"`，替代原来的 `[gloss repair background]`
- 未知选项被静默忽略，不污染 prompt
- 测试：`TestPromptTranslatesEnhancements`（断言自然语言输出且不含原始 key）+ `TestPromptIgnoresUnknownEnhancements`，全量通过

`moduleCounts` 的原始拼接已在待办 1 第一期中由 `moduleHint` 替代，本项即最后的粗糙拼接。

---

## 非代码项：AI 供应商密钥未配置

`aiProviderConfigured: false`——`BANANA_ROUTER_API_KEY` 未填，所有生图请求被 `AI_PROVIDER_UNAVAILABLE` 拦在工作台。这是**环境配置问题，不是代码缺失**：后端三条生成路径（gemini / openai-edit / openai-generation）均已实现，配上密钥即可跑通。配置方式见 `README.md` 的后端章节。
